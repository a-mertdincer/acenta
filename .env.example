# Copy this file to .env and set your values.
#
# Neon (serverless PostgreSQL):
#   DATABASE_URL: Dashboard → Connection string → "Pooled connection" (günlük uygulama için).
#   DIRECT_URL:   Dashboard → Connection string → "Direct connection" (migrate için gerekli).
#   Pooler kullanırken "prisma migrate" advisory lock timeout verir; DIRECT_URL bunu önler.
DATABASE_URL="postgresql://USER:PASSWORD@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require"
DIRECT_URL="postgresql://USER:PASSWORD@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"

# Google OAuth (NextAuth/Auth.js)
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"
AUTH_SECRET="your-random-auth-secret"

# DeepL API keys (Free tier — ~500k chars/month per key). Helper rotates on HTTP 456.
# Add as many as needed: DEEPL_API_KEY_1 … DEEPL_API_KEY_10
DEEPL_API_KEY_1=
DEEPL_API_KEY_2=
DEEPL_API_KEY_3=
DEEPL_API_KEY_4=

# Optional: paid Pro key (api.deepl.com — same helper appends after free keys)
DEEPL_API_KEY_PRO=
